# Screenshots

Add a dashboard screenshot here (e.g. `dashboard.png`) and link it from the root README.

Suggested capture: live SSE job board with Redis workers ≥ 1, one failed single-agent job and one succeeded multi-agent job, plus the published eval table (33% vs 100%).
